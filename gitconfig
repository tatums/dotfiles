[user]
  name = Tatum
  email = tatum@ashlandstudios.com
  signingKey = A82E70E4
[alias]
  d = diff
  co = checkout
  s = status
  st = status
  c = commit
  b = branch
  a = add
  l = log --oneline --abbrev-commit --graph
  ll = log --pretty=format:'%C(Yellow)%h %C(Reset) %ad %C(Cyan) %s %C(Green)(%an) %C(Reset)' --date=short
  repull = pull --rebase
  ctags = !.git/hooks/ctags
[core]
  excludesFile = ~/.gitignore
  editor = nvim
[url "ssh://git@github.com/"]
  insteadOf = https://github.com/
[init]
  defaultBranch = main
[pull]
  ff = only
[filter "lfs"]
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process
  required = true
[branch]
  # Show most recently changed branches first.
  sort = -committerdate
[commit]
  gpgsign = true
